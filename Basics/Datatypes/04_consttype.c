/*
4. 🔒 const

Challenge: Protect Configuration Data

const uint8_t config[5] = {1,2,3,4,5};

Task:

* Try to write a function that attempts to modify it:

void modify_config(const uint8_t *ptr);

Requirement:

* Show:
    1. What compiler allows
    2. What happens if you force cast and modify
* Explain in comments:
    * Why this is dangerous in embedded (Flash vs RAM)
    
*/

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
